#include "main.h"
/**
 * mul - A function that multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply first number
 * Return: Result of multiplication 'a * b'
 */
int mul(int a, int b)
{
return (a * b);
}
